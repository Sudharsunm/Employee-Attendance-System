# Employee-Attendance-System
steps to run the application
set-up instructions
1.create a sepaarte folder for the project
2.create sub folder named Frontend,Backend in the project folder
3.choose which opertion is to done first ethier Frontend or Backend
4.if backend, then follow these instructions and run it in the cmd
  =>cd backend
  =>npm init -y
  =>npm install express cors dotenv mongoose jsonwebtoken bcryptjs
  => create the main server file "server.js"
  =>create necessary folder as well as the file for and api
  =>ensure there is no error 
  =>finally run "npm start" to start the working of the server

FRONTEND
1.npm create vite@latest frontend
cd frontend
npm install axios react-router-dom
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
npm run dev

then api testing


FUTURE IMPLEMENTATION => "use docker for containerization"
